---
name: aios-event-analysis
description: 对已脱敏的 AIOS 客户反馈、日志、错误码和异常现象进行证据优先分析，按需查证钉钉知识库、BBS、Jira、Confluence、源码和外部官方资料。
---

# AIOS 事件分析

默认受众为 `internal`。用户明确要求销售口径时输出 `sales`，不得携带内部链接、客户标识或敏感操作细节。

## 流程

1. 整理当前事件事实：版本、组件、部署形态、操作路径、稳定错误信号和影响。
2. 缺少会改变结论的版本或错误信号时，只追问最小信息。
3. 形成去标识化故障指纹。
4. 首批查询知识库、BBS 和 Jira；涉及机制或版本时同批查询对应 CodeContext 源码。
5. Confluence 只作为研发说明；外部资料优先一手官方来源。
6. 将结论标为已确认、较可能、可能或证据缺失。

当前事件证据始终优先于历史案例。BBS 相似案例不能单独证明当前根因，Jira fixVersion 不能替代代码合入和发布快照确认。

## 闭环要求

完整结论应包含：现象、影响、最强证据、原因边界、建议动作、验证方法、风险和未完成查证。事件没有恢复或验证不足时不得写“已解决”。

本插件只生成分析和建议，不连接客户环境执行命令。
